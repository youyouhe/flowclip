FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies with specific order to resolve interactjs issue
RUN npm install interactjs --legacy-peer-deps && \
    npm install --legacy-peer-deps

# Expose port
EXPOSE 3000

# Start the development server
CMD ["npm", "run", "dev"]