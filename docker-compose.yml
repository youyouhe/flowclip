version: '3.3'

services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: flowclip-mysql
    ports:
      - "3307:3306"  # Using 3307 to avoid conflict with existing MySQL on 3306
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: youtube_slicer
      MYSQL_USER: youtube_user
      MYSQL_PASSWORD: youtube_password
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - flowclip-network
    command: --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "youtube_user", "-pyoutube_password"]
      timeout: 20s
      retries: 10
      interval: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"

  # Redis for caching and task queue
  redis:
    image: redis:7-alpine
    container_name: flowclip-redis
    # 移除端口映射，只在内部网络访问以增强安全性
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 1gb --maxmemory-policy allkeys-lru --protected-mode no
    networks:
      - flowclip-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      timeout: 5s
      retries: 10
      interval: 10s
      start_period: 30s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"

  # MinIO for file storage
  minio:
    image: minio/minio:latest
    container_name: flowclip-minio
    ports:
      - "9000:9000"  # Expose MinIO API port
      - "9001:9001"  # Expose MinIO Console port
    environment:
      MINIO_ROOT_USER: i4W5jAG1j9w2MheEQ7GmYEotBrkAaIPSmLRQa6Iruc0=  # 更安全的访问密钥
      MINIO_ROOT_PASSWORD: TcFA+qUwvCnikxANs7k/HX7oZz2zEjLo3RakL1kZt5k=  # 更安全的秘密密钥
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    networks:
      - flowclip-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      timeout: 5s
      retries: 10
      interval: 10s
      start_period: 30s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flowclip-backend
    ports:
      - "8001:8001"  # Expose backend API port for CapCut access
    environment:
      - DATABASE_URL=mysql+aiomysql://youtube_user:youtube_password@mysql:3306/youtube_slicer?charset=utf8mb4
      - REDIS_URL=redis://redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_PUBLIC_ENDPOINT=${PUBLIC_IP}:9000  # Use public endpoint for signed URLs
      - MINIO_ACCESS_KEY=i4W5jAG1j9w2MheEQ7GmYEotBrkAaIPSmLRQa6Iruc0=
      - MINIO_SECRET_KEY=TcFA+qUwvCnikxANs7k/HX7oZz2zEjLo3RakL1kZt5k=
      - MINIO_BUCKET_NAME=youtube-videos
      - SECRET_KEY=your-secret-key-change-in-production
      - DEBUG=true
      - FRONTEND_URL=http://frontend:3000
      - BACKEND_PUBLIC_URL=http://127.0.0.1:8001  # Public URL for CapCut access
    depends_on:
      - mysql
      - redis
      - minio
    volumes:
      - ./backend:/app
      - ./.env:/app/.env:ro
      - media_files:/app/media
      - /tmp/cookies:/tmp/cookies
      - /tmp:/tmp
    networks:
      - flowclip-network
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"

  # 独立TUS回调服务器
  callback-server:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flowclip-callback
    command: python callback_server.py
    ports:
      - "9090:9090"  # 固定端口用于TUS回调服务器
    environment:
      - REDIS_URL=redis://redis:6379
      - CALLBACK_HOST=0.0.0.0
      - CALLBACK_PORT=9090
      - REDIS_KEY_PREFIX=tus_callback
      - REDIS_RESULT_PREFIX=tus_result
      - REDIS_STATS_KEY=tus_callback_stats
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app  # 源码卷映射，便于开发调试
    networks:
      - flowclip-network
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9090/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # Celery Worker
  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flowclip-celery
    command: python start_celery.py worker --loglevel=info --concurrency=4
    # 移除端口映射，不再需要从celery worker暴露回调端口
    environment:
      - DATABASE_URL=mysql+aiomysql://youtube_user:youtube_password@mysql:3306/youtube_slicer?charset=utf8mb4
      - REDIS_URL=redis://redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_PUBLIC_ENDPOINT=${PUBLIC_IP}:9000
      - MINIO_ACCESS_KEY=i4W5jAG1j9w2MheEQ7GmYEotBrkAaIPSmLRQa6Iruc0=
      - MINIO_SECRET_KEY=TcFA+qUwvCnikxANs7k/HX7oZz2zEjLo3RakL1kZt5k=
      - MINIO_BUCKET_NAME=youtube-videos
      - SECRET_KEY=your-secret-key-change-in-production
      - PUBLIC_IP=${PUBLIC_IP}
      - DEBUG=true
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      backend:
        condition: service_started
    volumes:
      - ./backend:/app
      - ./.env:/app/.env:ro
      - media_files:/app/media
      - /tmp/cookies:/tmp/cookies
      - /tmp:/tmp
    networks:
      - flowclip-network
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "10"

  # Celery Beat (for scheduled tasks)
  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flowclip-celery-beat
    command: python start_celery.py beat --loglevel=info
    environment:
      - DATABASE_URL=mysql+aiomysql://youtube_user:youtube_password@mysql:3306/youtube_slicer?charset=utf8mb4
      - REDIS_URL=redis://redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_PUBLIC_ENDPOINT=${PUBLIC_IP}:9000
      - MINIO_ACCESS_KEY=i4W5jAG1j9w2MheEQ7GmYEotBrkAaIPSmLRQa6Iruc0=
      - MINIO_SECRET_KEY=TcFA+qUwvCnikxANs7k/HX7oZz2zEjLo3RakL1kZt5k=
      - MINIO_BUCKET_NAME=youtube-videos
      - SECRET_KEY=your-secret-key-change-in-production
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      backend:
        condition: service_started
    volumes:
      - ./backend:/app
      - ./.env:/app/.env:ro
    networks:
      - flowclip-network
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: flowclip-frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=/api
      - NODE_ENV=development
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
    networks:
      - flowclip-network
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"
    # Ensure dependencies are installed before starting dev server
    entrypoint: ["/bin/sh", "-c", "npm install --legacy-peer-deps && npm run dev"]

  # MCP Server
  mcp-server:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flowclip-mcp-server
    ports:
      - "8002:8002"  # Expose MCP server port
    environment:
      - DATABASE_URL=mysql+aiomysql://youtube_user:youtube_password@mysql:3306/youtube_slicer?charset=utf8mb4
      - REDIS_URL=redis://redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_PUBLIC_ENDPOINT=${PUBLIC_IP}:9000
      - MINIO_ACCESS_KEY=i4W5jAG1j9w2MheEQ7GmYEotBrkAaIPSmLRQa6Iruc0=
      - MINIO_SECRET_KEY=TcFA+qUwvCnikxANs7k/HX7oZz2zEjLo3RakL1kZt5k=
      - MINIO_BUCKET_NAME=youtube-videos
      - SECRET_KEY=your-secret-key-change-in-production
      - DEBUG=true
      - FRONTEND_URL=http://frontend:3000
    depends_on:
      - mysql
      - redis
      - minio
      - backend
    volumes:
      - ./backend:/app
      - ./.env:/app/.env:ro
      - media_files:/app/media
      - /tmp/cookies:/tmp/cookies
      - /tmp:/tmp
    networks:
      - flowclip-network
    command: python run_mcp_server_complete.py
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "2"

volumes:
  redis_data:
  minio_data:
  media_files:
  mysql_data:

networks:
  flowclip-network:
    driver: bridge
